# This directory holds live data (clients, users, portal-state, etc.).
# *.json files here are not in git so deploys do not overwrite production data.
# The server creates missing JSON files on first run.
